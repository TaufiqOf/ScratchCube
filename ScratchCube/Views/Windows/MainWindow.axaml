<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:ScratchCube.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:windows="clr-namespace:ScratchCube.ViewModels.Windows"
        xmlns:controls="clr-namespace:ScratchCube.Views.Controls"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="ScratchCube.Views.Windows.MainWindow"
        x:DataType="windows:MainViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="ScratchCube">

    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
             to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        <windows:MainViewModel />
    </Design.DataContext>
    <Grid ColumnDefinitions="200,*">
        <controls:MenuControlView Grid.Column="0" DataContext="{Binding MenuControlViewModel}"/>
        <ContentControl Grid.Column="1" Content="{Binding CurrentPage}"/>
    </Grid>
</Window>
