<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:component="clr-namespace:ScratchDocker.ViewModels.Controls"
             xmlns:fluent="clr-namespace:FluentIcons.Avalonia.Fluent;assembly=FluentIcons.Avalonia.Fluent"
             x:DataType="component:MenuControlViewModel"
             x:Class="ScratchDocker.Views.Controls.MenuControlView">

    <Border Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
            BorderThickness="0,0,1,0">

        <Grid RowDefinitions="Auto,*,Auto">

            <!-- App header -->
            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        Margin="16,20,16,20"
                        Spacing="10">

                <fluent:FluentIcon Icon="Box"
                                   Width="22"
                                   Height="22"
                                   Foreground="{DynamicResource SystemControlAccentBrush}" />

                <TextBlock Text="ScratchDocker"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>


            <!-- Navigation -->
            <ListBox Grid.Row="1"
                     ItemsSource="{Binding MenuItems}"
                     SelectedItem="{Binding SelectedMenuItem}"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="8,0">

                <ListBox.Styles>

                    <Style Selector="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,2" />
                        <Setter Property="HorizontalContentAlignment"
                                Value="Stretch" />
                    </Style>

                    <!-- Hover -->
                    <Style Selector="ListBoxItem:pointerover">
                        <Setter Property="Background"
                                Value="{DynamicResource SystemControlHighlightListLowBrush}" />
                    </Style>

                    <!-- Selected -->
                    <Style Selector="ListBoxItem:selected">
                        <Setter Property="Background"
                                Value="{DynamicResource SystemControlHighlightListAccentLowBrush}" />
                    </Style>

                    <!-- Selected + hover -->
                    <Style Selector="ListBoxItem:selected:pointerover">
                        <Setter Property="Background"
                                Value="{DynamicResource SystemControlHighlightListAccentMediumBrush}" />
                    </Style>

                </ListBox.Styles>

                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <Border CornerRadius="6"
                                Padding="10,9">

                            <Grid ColumnDefinitions="Auto,*"
                                  VerticalAlignment="Center">

                                <fluent:FluentIcon Icon="{Binding Icon}"
                                                   Width="18"
                                                   Height="18"
                                                   Margin="0,0,12,0"
                                                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />

                                <TextBlock Grid.Column="1"
                                           Text="{Binding Name}"
                                           FontSize="14"
                                           Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                                           VerticalAlignment="Center" />

                            </Grid>

                        </Border>

                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>


            <!-- Engine status -->
            <Border Grid.Row="2"
                    Margin="12,10,12,14"
                    Padding="10"
                    Background="{DynamicResource SystemControlBackgroundBaseLowBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="7">

                <Grid ColumnDefinitions="Auto,*,Auto">

                    <Ellipse Width="8"
                             Height="8"
                             Fill="{Binding EngineStatusColor}"
                             VerticalAlignment="Center"
                             Margin="0,0,8,0" />

                    <TextBlock Grid.Column="1"
                               Text="{Binding EngineStatusText}"
                               FontSize="12"
                               Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                               VerticalAlignment="Center" />

                    <!-- More button -->
                    <Button Grid.Column="2"
                            Width="28"
                            Height="28"
                            Padding="0"
                            Background="Transparent"
                            BorderThickness="0"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center">

                        <Button.Content>
                            <fluent:FluentIcon Icon="MoreHorizontal"
                                               Width="16"
                                               Height="16"
                                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                        </Button.Content>

                        <Button.Flyout>
                            <Flyout Placement="TopEdgeAlignedRight">

                                <StackPanel Spacing="4">

                                    <Button Content="Start Engine"
                                            Command="{Binding StartEngineCommand}" />

                                    <Button Content="Stop Engine"
                                            Command="{Binding StopEngineCommand}" />

                                    <Separator />

                                    <Button Content="Engine Settings"
                                            Command="{Binding OpenEngineSettingsCommand}" />

                                </StackPanel>

                            </Flyout>
                        </Button.Flyout>

                    </Button>

                </Grid>
            </Border>

        </Grid>

    </Border>

</UserControl>